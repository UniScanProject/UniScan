<Project>
    <PropertyGroup>
        <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
    </PropertyGroup>

    <Target Name="AppendHash"
            AfterTargets="GetAssemblyVersion"
            BeforeTargets="GetAssemblyAttributes"
            Condition="!$(MSBuildProjectDirectory.Contains('External'))">

        <Exec Command="git tag --points-at HEAD -l &quot;$(MSBuildProjectName)/$(AssemblyVersion)&quot;"
              WorkingDirectory="$(MSBuildThisFileDirectory)"
              ConsoleToMSBuild="true"
              StandardOutputImportance="Low"
              IgnoreExitCode="true">
            <Output TaskParameter="ConsoleOutput" PropertyName="Tag" />
        </Exec>

        <Exec Command="git rev-parse --short HEAD"
              WorkingDirectory="$(MSBuildThisFileDirectory)"
              ConsoleToMSBuild="true"
              StandardOutputImportance="Low"
              IgnoreExitCode="true">
            <Output TaskParameter="ConsoleOutput" PropertyName="Hash" />
        </Exec>

        <Exec Command="git branch --show-current"
              WorkingDirectory="$(MSBuildThisFileDirectory)"
              ConsoleToMSBuild="true"
              StandardOutputImportance="Low"
              IgnoreExitCode="true">
            <Output TaskParameter="ConsoleOutput" PropertyName="Branch" />
        </Exec>
        
        <PropertyGroup>
            <BranchTag Condition="'$(Branch)' != 'main' and '$(Branch)' != ''">-$(Branch.Replace('/', '-').Replace('_', '-'))</BranchTag>
        </PropertyGroup>
        
        <PropertyGroup Condition="'$(Tag)' == '' and '$(Hash)' != ''">
            <InformationalVersion>$(AssemblyVersion)$(BranchTag)+$(Hash)</InformationalVersion>
        </PropertyGroup>

        <PropertyGroup Condition="'$(Tag)' != '' or '$(Hash)' == ''">
            <InformationalVersion>$(AssemblyVersion)$(BranchTag)</InformationalVersion>
        </PropertyGroup>
    </Target>
</Project>